@using FruitsTraceabilitySystem.Application.ViewModels.Locations

@model IEnumerable<LocationViewModel>


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Location List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Location" asp-action="Created" class="btn btn-primary"><i class="far fa-plus-circle"></i> &nbsp; New Location</a>
        </div>
    </div>
</div>
<br/>
<br/>
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Location Id
            </th>
             <th>
                Location Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var location in Model)
        {
            <tr>
                <td width="35%">
                    @location.Id
                </td> 
                <td width="30%">
                    @location.PlaceName
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Location" class="btn btn-primary mx-2" asp-route-id="@location.Id" asp-action="Edit"><i class="fad fa-file-edit"></i></a>
                        <a asp-controller="Location" class="btn btn-danger mx-2" asp-route-id="@location.Id" asp-action="Delete"><i class="fad fa-trash-alt"></i></a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>