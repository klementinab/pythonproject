@using FruitsTraceabilitySystem.Application.ViewModels.Categories
@model IEnumerable<CategoryViewModel>


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Category List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Category" asp-action="Created" class="btn btn-primary"><i class="far fa-plus-circle"></i> &nbsp; New Category</a>
        </div>
    </div>
</div>
<br/>
<br/>
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Category Id
            </th>
             <th>
                Category Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var category in Model)
        {
            <tr>
                <td width="35%">
                    @category.Id
                </td> 
                <td width="30%">
                    @category.Name
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Category" class="btn btn-primary mx-2" asp-route-id="@category.Id" asp-action="Edit"><i class="fad fa-file-edit"></i></a>
                        <a asp-controller="Category" class="btn btn-danger mx-2" asp-route-id="@category.Id" asp-action="Delete"><i class="fad fa-trash-alt"></i></a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>